# Test Task
# Для старта проекта можно собрать его с помощью docker-compose, либо отдельно стартануть бэк и фронт с помощью npm
## Описание API бэка расположено в http://localhost:5000/api-docs
## Сервер раздает 4 статичные страницы расположенные в папке public/staticPages
  Страницы прокинуты в volume docker'а и их можно редактировать после сборки проекта
## Также раздается шаблон, который на каждый запрос отдает новую страницу с текущим timestamp
## При запуске сервер инициализирует словарь за вчера
  начальные ссылки расположены в /backend/src/data/db.ts (const testLinks)
## Добавлять на клиенте можно только в CurrentSites 

